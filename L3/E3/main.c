#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int calc_hist(int vect[]);
int main (){
    int vect[]= {0, 5, 2, 400};
    printf("%d", calc_hist(vect));
    return EXIT_SUCCESS;
}

int calc_hist(int vect[]){
    int i;
    for(i = 0; i < sizeof(vect); i++){
    }
    return i;
}